# if you want to use this setting, copy this into `.env` file
# and refresh your bash, and run `npm run ios` or `npm run android`

API_URL=https://api.popo.poapper.club
PAXI_API_URL=https://api.paxi.popo.poapper.club
