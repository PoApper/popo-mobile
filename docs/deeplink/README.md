# 딥링크 설정파일들
POPO EC2 머신에 있는(`/var/www/popo/room.html`, `/var/www/popo-dev/room.html`) 파일로, 프로젝트에서는 쓰이지 않고 정보 제공을 위해 첨부했습니다. `/room/*`경로로 딥링크 요청이 들어왔을 때 아래와 같은 역할을 수행합니다.
- 카카오톡과 같은 In-App 브라우저에서 카풀 방 공유 링크를 눌렀을 때 앱으로 이어줌 
- 디바이스에 앱이 깔려있지 않으면, 플레이/앱스토어로 리다이렉트 시켜줌